<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>For CHOUBAO</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Poppins:wght@300;400&display=swap" rel="stylesheet">

<style>
    body {
        margin: 0;
        padding: 0;
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(135deg, #1a1a1a, #2d0b2e);
        color: #fff;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
    }

    .container {
        text-align: center;
        max-width: 500px;
        padding: 40px;
        backdrop-filter: blur(15px);
        background: rgba(255, 255, 255, 0.05);
        border-radius: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.4);
        animation: fadeIn 2s ease;
    }

    h1 {
        font-family: 'Playfair Display', serif;
        font-size: 36px;
        margin-bottom: 10px;
        letter-spacing: 2px;
    }

    h2 {
        font-weight: 300;
        margin-top: 0;
        color: #ffb6c1;
    }

    p {
        margin: 25px 0;
        font-size: 18px;
        line-height: 1.6;
    }

    #countdown {
        font-size: 20px;
        margin-bottom: 30px;
        color: #ffd6e0;
        letter-spacing: 1px;
    }

    button {
        padding: 12px 30px;
        border: none;
        border-radius: 30px;
        font-size: 16px;
        cursor: pointer;
        margin: 10px;
        transition: 0.3s ease;
    }

    #yesBtn {
        background: #ff4b8b;
        color: white;
    }

    #yesBtn:hover {
        background: #ff2f75;
        transform: scale(1.05);
    }

    #noBtn {
        background: transparent;
        color: #ccc;
        border: 1px solid #555;
    }

    #response {
        margin-top: 20px;
        font-size: 18px;
        color: #ffb6c1;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* floating lights */
    .light {
        position: absolute;
        width: 6px;
        height: 6px;
        background: pink;
        border-radius: 50%;
        opacity: 0.6;
        animation: float 10s linear infinite;
    }

    @keyframes float {
        from { transform: translateY(100vh); opacity: 0; }
        to { transform: translateY(-10vh); opacity: 1; }
    }
</style>
</head>
<body>

<div class="container">
    <h1>CHOUBAO</h1>
    <h2>Will you be my Valentine?</h2>

    <p>
        I may not be asking you in person yet…  
        but I know one thing for sure —  
        I want February 14th to be ours. 🤍
    </p>

    <div id="countdown"></div>

    <button id="yesBtn" onclick="sayYes()">Yes 🤍</button>
    <button id="noBtn" onclick="sayNo()">No</button>

    <div id="response"></div>
</div>

<script>
// Countdown Timer
const targetDate = new Date("February 14, 2026 00:00:00").getTime();

const countdown = setInterval(function() {
    const now = new Date().getTime();
    const distance = targetDate - now;

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    document.getElementById("countdown").innerHTML =
        days + " days " + hours + " hrs " + minutes + " mins " + seconds + " secs";

    if (distance < 0) {
        clearInterval(countdown);
        document.getElementById("countdown").innerHTML = "It’s our day 🤍";
    }
}, 1000);

// Button interactions
function sayYes() {
    document.getElementById("response").innerHTML =
        "I can’t wait for our Valentine’s date, CHOUBAO 🤍✨";
}

function sayNo() {
    document.getElementById("response").innerHTML =
        "I’ll ask you again tomorrow 😉";
}

// Floating lights generator
for (let i = 0; i < 30; i++) {
    let light = document.createElement("div");
    light.className = "light";
    light.style.left = Math.random() * 100 + "vw";
    light.style.animationDuration = (Math.random() * 5 + 5) + "s";
    document.body.appendChild(light);
}
</script>

</body>
</html>